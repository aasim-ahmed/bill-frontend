# hooks/

Custom React hooks that encapsulate reusable stateful logic.

Rules:
- Every file must start with `use` (e.g. `useBills.js`, `usePrinter.js`).
- Hooks must NOT contain JSX.
- Hooks should be pure logic extractions from components.
